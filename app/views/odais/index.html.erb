<div class="main posts-index">
  <div class="container">
    <% @odais.each do |odai| %>
      <div class="posts-index-item">
        <%= link_to(odai.content, "/odais/#{odai.id}") %>
      </div>
    <% end %>
  </div>
</div>
